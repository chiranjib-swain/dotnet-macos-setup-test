name: Reproduce setup-dotnet v3 Issue 387

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Setup .NET 6
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '6.0.x'

    - name: Display .NET 6 version
      run: dotnet --version

    - name: Restore dependencies for .NET 6
      run: dotnet restore

    - name: Build with .NET 6
      run: dotnet build --no-restore

    - name: Test with .NET 6
      run: dotnet test --no-build --verbosity normal

    - name: Setup .NET 7
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '7.0.x'

    - name: Display .NET 7 version
      run: dotnet --version

    - name: Restore dependencies for .NET 7
      run: dotnet restore

    - name: Build with .NET 7
      run: dotnet build --no-restore

    - name: Test with .NET 7
      run: dotnet test --no-build --verbosity normal
